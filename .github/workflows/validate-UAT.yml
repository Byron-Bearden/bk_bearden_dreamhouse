name: Validate PR - UAT

on:
  pull_request:
    branches:
      - UAT

permissions:
  contents: read

jobs:
  validate:
    uses: ./.github/workflows/validate.yml

    with:
      environment: uat
      use-delta: true
      test-level: RunLocalTests